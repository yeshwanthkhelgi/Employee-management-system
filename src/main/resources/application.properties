spring.application.name=springboot

# Database Configuration (using GitHub secrets)
spring.datasource.url=jdbc:mysql://localhost:3306/${MYSQL_DATABASE}
spring.datasource.username=root  # ✅ Use root user directly
spring.datasource.password=${MYSQL_ROOT_PASSWORD}  # Use the root password

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto=update

# Logging
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type=TRACE
spring.devtools.add-properties=false
